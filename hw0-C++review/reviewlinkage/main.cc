#include "Fraction.hh"

int main() {
	Fraction f1(1,2);
	Fraction f2(1,3);
	f3.test(); // call a method
	Fraction f3 = f1 + f2; // use an overloaded operator
	cout << f3 << '\n';    // use overloaded operator <<
}
